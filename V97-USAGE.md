# 🎉 Particle Spine Exporter v97 - Usage Guide

## ✅ Что было сделано

Версия v96 (4135 строк) была **разбита на модульную структуру v97** для удобства разработки, с возможностью **автоматической сборки в standalone файл** для использования.

---

## 📁 Структура проекта

```
SpineParticleExporter/
├── particle-spine-exporter-v97/       ← Модульная версия (для разработки)
│   ├── types.ts         (7.5 KB)     - Типы, интерфейсы, константы
│   ├── utils.ts         (3.2 KB)     - Утилиты (noise, curves, math)
│   ├── core.ts          (23 KB)      - ParticleSystem класс
│   ├── export.ts        (34 KB)      - Экспорт (sprites, atlas, Spine JSON)
│   ├── components.tsx   (31 KB)      - React UI компоненты
│   ├── index.tsx        (58 KB)      - Главный компонент
│   └── README.md                     - Документация модулей
│
├── particle-spine-exporter_alpha_v97.tsx  ← Standalone версия (для использования)
│
├── build-standalone.js                - Скрипт сборки
├── split-to-modules.js                - Скрипт разбиения
├── package.json                       - NPM конфигурация
└── V97-USAGE.md                       - Эта инструкция
```

---

## 🚀 Как использовать

### Вариант 1️⃣: Использование готового standalone файла

**Для проверки работы используйте:**

```
particle-spine-exporter_alpha_v97.tsx
```

Этот файл полностью самодостаточен и работает точно так же, как v96.
Просто импортируйте его в ваш проект React.

---

### Вариант 2️⃣: Разработка в модулях

**Для редактирования кода работайте с модулями:**

```bash
cd particle-spine-exporter-v97/
```

Редактируйте нужные файлы:
- 🎨 Меняете UI? → `components.tsx`
- ⚙️ Меняете физику? → `core.ts`
- 📤 Меняете экспорт? → `export.ts`
- 🔧 Меняете типы? → `types.ts`

**После изменений соберите standalone версию:**

```bash
npm run build
```

Это создаст обновленный `particle-spine-exporter_alpha_v97.tsx`

---

## 🎯 Основные команды

| Команда | Действие |
|---------|----------|
| `npm run build` | Собрать standalone v97 из модулей |
| `npm run split` | Пересоздать модули из v96 (если нужно) |

---

## 📝 Рабочий процесс

### Сценарий 1: Быстрая проверка
```bash
# Просто используйте готовый файл
open particle-spine-exporter_alpha_v97.tsx
```

### Сценарий 2: Разработка новой функции
```bash
# 1. Редактируйте нужные модули
vim particle-spine-exporter-v97/core.ts

# 2. Соберите standalone
npm run build

# 3. Проверьте результат
# (используйте particle-spine-exporter_alpha_v97.tsx в вашем проекте)
```

### Сценарий 3: Массовый рефакторинг
```bash
# Работайте со всеми модулями одновременно
cd particle-spine-exporter-v97/

# После всех изменений
cd ..
npm run build
```

---

## 🔍 Детали модулей

### `types.ts` (~300 строк)
- Все TypeScript интерфейсы
- DEFAULT_CURVE_PRESETS
- DEFAULT_SETTINGS
- Конфигурационные константы

### `utils.ts` (~150 строк)
- `simpleNoise()` - генерация шума
- `evaluateCurve()` - интерполяция кривых
- `evaluateColorGradient()` - интерполяция цветов
- Вспомогательные математические функции

### `core.ts` (~700 строк)
- **`ParticleSystem` класс** - ядро симуляции
- Спавн частиц
- Физика (гравитация, drag, vortex, noise)
- Рендеринг в canvas

### `export.ts` (~1200 строк)
- `createParticleSprite()` - генерация текстур
- `createParticleAtlas()` - упаковка атласа
- `bakeParticleAnimation()` - запись анимации
- `generateSpineJSON()` - создание Spine 4.2 JSON
- `SimpleZip` класс - упаковка в ZIP

### `components.tsx` (~1000 строк)
- `ColorPicker` - HSV color wheel
- `ColorGradientEditor` - редактор градиента
- `CurveEditor` - редактор кривых анимации
- `Timeline` - контролы воспроизведения
- `RangeInput` - min/max редактор
- `CollapsibleSection` - сворачиваемые секции

### `index.tsx` (~800 строк)
- **`ParticleSpineExporter`** - главный компонент
- State management
- Canvas rendering loop
- UI layout и взаимодействие

---

## 🎨 Преимущества модульной структуры

| Проблема | Было (v96) | Стало (v97) |
|----------|------------|-------------|
| Навигация | 4135 строк в одном файле | 6 файлов по 150-1200 строк |
| Поиск кода | Ctrl+F по всему файлу | Знаешь в каком модуле искать |
| Редактирование UI | Ищи среди 4000 строк | Открой `components.tsx` |
| Изменение физики | Ищи ParticleSystem | Открой `core.ts` |
| Конфликты в Git | Весь файл конфликтует | Только нужные модули |
| Тестирование | Сложно изолировать | Модули независимы |

---

## 🔄 Сравнение v96 vs v97

### v96 (Монолит)
```
✅ Готов к использованию сразу
✅ Один файл - просто распространять
❌ 4135 строк - сложно редактировать
❌ Трудно найти нужный код
❌ Конфликты при командной работе
```

### v97 (Модули + Standalone)
```
✅ Легко редактировать (модули)
✅ Быстрая навигация по коду
✅ Изолированные изменения
✅ Готовый standalone файл (после сборки)
✅ Лучше для командной работы
⚠️ Нужен шаг сборки (npm run build)
```

---

## 🛠️ Технические детали

### Как работает сборка

`build-standalone.js` делает следующее:
1. Читает все 6 модулей
2. Удаляет `import`/`export` statements
3. Объединяет код в правильном порядке:
   - Header + React imports
   - Types → Utils → Components → Core → Export → Main
4. Добавляет разделители для читаемости
5. Создает `particle-spine-exporter_alpha_v97.tsx`

### Размеры файлов

```
v96 (монолит):  153 KB  4135 строк
v97 (standalone): 154 KB  4175 строк  (+40 строк за счёт разделителей)

Модули:
  types.ts:       7.5 KB
  utils.ts:       3.2 KB
  core.ts:        23 KB
  export.ts:      34 KB
  components.tsx: 31 KB
  index.tsx:      58 KB
  -------------------------
  Total:          157 KB  (немного больше из-за дублирования импортов)
```

---

## 📚 Дальнейшие шаги

### Рекомендации по разработке

1. **Для мелких правок** - редактируйте standalone v97.tsx напрямую
2. **Для больших изменений** - работайте с модулями, потом собирайте
3. **Для новых фич** - добавляйте в соответствующий модуль (core, export, components)
4. **Перед коммитом** - всегда запускайте `npm run build`

### Что можно улучшить в будущем

- [ ] Добавить TypeScript компиляцию с проверкой типов
- [ ] Настроить ESLint для модулей
- [ ] Добавить unit тесты для utils и core
- [ ] Создать watch режим (автосборка при изменениях)
- [ ] Добавить minification для production сборки

---

## ❓ FAQ

### Q: Какой файл использовать для проверки?
**A:** Используйте `particle-spine-exporter_alpha_v97.tsx` - это готовый standalone файл.

### Q: Как внести изменения в код?
**A:** Редактируйте модули в `particle-spine-exporter-v97/`, потом запустите `npm run build`.

### Q: Можно ли редактировать напрямую v97.tsx?
**A:** Да, но изменения не попадут в модули. Лучше редактировать модули и пересобирать.

### Q: Как вернуться к v96?
**A:** Просто используйте `particle-spine-exporter_alpha_v96.tsx` - он никуда не делся.

### Q: Standalone версия работает так же как v96?
**A:** Да, функционально они идентичны. v97 - это просто реорганизованный код v96.

### Q: Нужен ли build step для использования?
**A:** Нет для использования (v97.tsx уже собран). Да для разработки (после изменений в модулях).

---

## 🎉 Готово!

Теперь у вас есть:
- ✅ Модульная структура для удобной разработки
- ✅ Standalone файл для использования
- ✅ Автоматическая сборка одной командой
- ✅ Полная документация

**Начните с проверки:**
```bash
# Посмотрите на standalone версию
cat particle-spine-exporter_alpha_v97.tsx | head -20

# Или откройте модули
ls -lh particle-spine-exporter-v97/
```

---

**Версия:** v97.0.0
**Дата:** 2025-11-25
**Основано на:** v96 (4135 строк)
